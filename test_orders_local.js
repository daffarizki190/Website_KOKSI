import pg from 'pg'; const { Client } = pg; const localUrl = 'postgresql://postgres:postgres@localhost:5432/koksi_db'; const client = new Client({connectionString: localUrl}); async function run() { await client.connect(); const res = await client.query('SELECT * FROM orders ORDER BY created_at DESC LIMIT 5;'); console.log('LOCAL ORDERS:', res.rows); await client.end(); } run().catch(console.error);
